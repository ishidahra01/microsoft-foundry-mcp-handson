# CopilotKit -> Foundry Agent (AG-UI) configuration
# Preferred: AG-UI endpoint URL of your Foundry/Microsoft Agent Framework agent.
FOUNDRY_AGENT_URL=https://your-agent-url.example.com/agui

# Backward-compatible fallback URL variable.
# AGENT_URL=https://your-agent-url.example.com/agui
# FOUNDRY_ENDPOINT=https://your-agent-url.example.com/agui

# Auth mode:
# - entra  : acquire Microsoft Entra token with DefaultAzureCredential (recommended)
# - bearer : use static bearer token from FOUNDRY_BEARER_TOKEN
# - none   : send no Authorization header
FOUNDRY_AUTH_MODE=entra

# Scope used when FOUNDRY_AUTH_MODE=entra
FOUNDRY_SCOPE=https://ai.azure.com/.default

# Required only when FOUNDRY_AUTH_MODE=bearer
# FOUNDRY_BEARER_TOKEN=your_bearer_token_here

# Optional: agent key in CopilotRuntime
COPILOTKIT_AGENT_NAME=foundry

# Optional: Application Insights
APPLICATIONINSIGHTS_CONNECTION_STRING=
